# Native project module

The minimal `ZombieSeasons` game module exists so UnrealBuildTool can generate IDE project files for this formerly Blueprint-only project. Runtime gameplay integration remains isolated in the `ZombieSeasonsRuntime` plugin.
